Use scaled dot product for scaling and to avoid vanishing gradients
Multiple query vectors
Separate key and value vectors
Self attention is permutation equi-variant
Self-attention enables models to reason within a sequence, building contextual understanding. Cross-attention connects different streams of information, allowing for alignment, fusion, and grounding.
Masked, multi-headed variants
